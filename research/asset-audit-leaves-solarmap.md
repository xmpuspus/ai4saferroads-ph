# Asset audit — leaves-ph + solar-map-ph -> ADB "AI for Safer Roads"

Extraction-only audit. Goal: reuse the SolarMap/TreeCover CNN-tile-classifier pattern for the C4 Mapillary street-view CV layer (read each road segment's frames -> sidewalks/crossings/median/lane-width/sign presence -> infer a self-explaining design speed vs posted limit), plus the SA-key-first GEE init for any land-cover layer, plus MODEL_CARD + deterministic-build discipline.

Date: 2026-06-25. Search restricted to the two repo dirs. No imagery/tile traversal.

## Credential check (must be personal only)

- Both repos carry `.ee-key.json` (gitignored, `chmod 600`). BOTH resolve to the SAME personal civic service account:
  - `client_email: 74080701912-compute@developer.gserviceaccount.com`
  - `project_id: poised-honor-217909` (Xavier's personal leaves-ph GEE project, matches the CLAUDE.md "personal Earth Engine key only" rule)
- NO Boost / Attic / work credential found. The `grep boost` hits are the ML term `HistGradientBoostingRegressor` / "gradient-boosted" / "built-up", not Boost Capital.
- **Transplant rule:** copy `_gee_init.py` and drop the same personal `.ee-key.json` into the new repo root (gitignored). Never a work GCP project.

## Asset table

| Repo | Asset | File | What | How-to-transplant |
|---|---|---|---|---|
| leaves-ph | SA-key-first GEE init | `pipeline/_gee_init.py` | 3-source EE auth (env var -> repo `.ee-key.json` -> interactive), idempotent `init()`, `ncr_geometry()` helper | Copy verbatim for the GEE land-cover layer (ESA WorldCover / Dynamic World built-up + landuse per road segment). Rename `LEAVES_PH_EE_KEY` env var; keep the same personal key path. |
| solar-map-ph | **CNN tile-scan engine (the core C4 pattern)** | `detection/scan/ncr_scan.py` | grid->fetch->CLIP-embed->LR-score->stream-JSONL->aggregate-GeoJSON; resumable, threaded fetch, deferred torch import, tiered thresholds (high 0.85 / candidate 0.70) | This IS the street-view classifier skeleton. Replace `fetch_tile` (Esri bbox export) with a Mapillary frame fetch per road-segment node; keep `load_clip`/`embed_batch`/`predict_proba`/JSONL-resume/aggregate as-is. Each road segment = "tile"; score = design-speed-feature presence. |
| solar-map-ph | Region-parametrized scan + landcover prefilter | `detection/scan/region_scan.py` | Same engine but `--region` reads bbox from `regions.json`; ESA WorldCover built-up prefilter skips ocean/forest cells before burning fetch quota | Reuse the `regions.json`-driven config + prefilter pattern to limit Mapillary calls to segments inside the MM road network bbox (or to built-up cells). The prefilter = direct reuse of the GEE landcover layer. |
| solar-map-ph | CLIP embed primitives | `ncr_scan.py:load_clip()` / `embed_batch()` | `openai/clip-vit-large-patch14` frozen, L2-normalized 768-d image embeddings, MPS/CUDA/CPU auto-select | Drop-in feature extractor for street-view frames. Same 768-d vector -> same LR head. Swap nothing. |
| solar-map-ph | **Classifier train + LOSO protocol** | `detection/train/train_classifier.py` | LogisticRegression on CLIP embeddings; group-aware leave-one-source-out validation; threshold sweep maximizing F1; saves `{clf, threshold, feature_dim}` joblib + metrics JSON | Train the design-speed-feature head (e.g. "has sidewalk", "has crossing", "has median") the same way. LOSO-by-road-corridor prevents adjacent-frame leakage exactly as LOSO-by-source does for tiles. Multi-label = one head per feature or a multi-output variant. |
| solar-map-ph | Region-stratified retrain + per-domain calibration | `detection/train/v5_region_holdout/train_calibrated_v5.py`, `build_region_holdout.py`, `holdout_split.py` | Platt-sigmoid per-domain calibration on a scan-realistic holdout; degeneracy guard; honest "uncalibrated/candidate" fallback for low-n | If MM corridors differ (EDSA vs barangay road), calibrate per road-class. Reuse the holdout-split + Platt + degeneracy-guard code to avoid over-claiming precision on under-sampled road types. |
| solar-map-ph | kWp/area estimate via SAM mask | `detection/scan/sam_panel_segments.py`, `region_sam_kwp.py`, `assemble_per_building.py` | SAM ViT-B segments the panel polygon inside a detection tile, area->scalar estimate, two-stage (area+colour gate then CLIP+clf semantic check) | Reusable to measure **lane width / sidewalk width / median width** from a frame: SAM-segment the road surface or sidewalk polygon, convert pixel area to meters. Same two-stage verify pattern guards against mis-segmentation. |
| both | MODEL_CARD format | `solar-map-ph/MODEL_CARD.md`, `leaves-ph/MODEL_CARD.md` | Field table (encoder, head, threshold, repro sha256) + "what changed/why" + training data counts + per-domain performance + intended-use/out-of-scope + known biases + ethics/privacy + load snippet + repro commands + citation | Direct template for the C4 model card. Critical for ADB: the **intended-use / out-of-scope / "not for enforcement against an individual"** section transfers 1:1 to a road-safety context (self-explaining-speed is advisory, not a citation tool). |
| solar-map-ph | Deterministic hash-verify build chain | `Makefile` (`hash`, `hash-verify`, `EXPECTED_HASH`), `MODEL_CARD.md` repro section | `make`-ordered dataset->train->calibrate->scan->aggregate; sha256-prefix pin on the joblib; pinned deps (`scikit-learn==1.7.2`, `joblib==1.5.2`, `numpy==1.26.4`) make bytes reproducible; `make status` partial-state inventory | Copy the Makefile target structure + `hash-verify` + pinned-deps discipline. The byte-pin catches dependency drift in the trained head; `make status` is great for crash-recovery during long Mapillary scans. |
| leaves-ph | Release gate runner | `scripts/verify_release.py` (ports `solar-map-ph/scripts/verify_v11_release.py`) | N-PASS/0-FAIL gate: em-dash sweep, AI-jargon regex, data-honesty checks over MD + `*.astro`, run before tag/push/deploy | Reuse as the pre-ship gate. The AI-jargon + em-dash sweeps satisfy the civic-honesty/no-AI-tell rules ADB-facing copy needs. |
| solar-map-ph | PII / no-leak release checks | `scripts/check_region_no_pii.py`, `scripts/check_no_residential_leaks.py`, `docs/privacy-impact-assessment.md` | Asserts every output GeoJSON is point-tile only, no address/owner fields; PIA doc | Road segments are public infrastructure (lower PII risk than rooftops), but reuse the "assert output schema carries no PII field" gate + PIA template for the Mapillary layer (frames can contain plates/faces -> document blurring reliance on Mapillary's own pipeline). |
| both | Static Astro + MapLibre site build | `leaves-ph/site/vercel.json`, `site/package.json`; solar-map-ph `site/` (Astro + MapLibre + Tailwind) | Astro static `output: "static"`, MapLibre-gl + pmtiles, build-time stats read from committed GeoJSON (no hardcoded numbers), strict CSP + security headers in `vercel.json`, ACAO on `/data/*` | Direct template for the speed-limit map UI. `vercel.json` security-headers + per-`/data/*` cache/CORS block is copy-paste. Build-time-stats pattern (`site/src/data/stats.ts`) keeps the "X segments where design-speed < posted" headline from drifting. |
| solar-map-ph | Esri tile fetcher (threaded, retrying, cached) | `ncr_scan.py:fetch_tile()` / `region_scan.py:fetch_tile()` | `urllib` GET to Esri World Imagery export, 3x retry w/ backoff, size-sanity check, disk cache, custom User-Agent | If a satellite tile is ever needed alongside street-view (e.g. intersection top-down), reuse directly. The retry/cache/UA pattern also templates the Mapillary frame fetcher. |
| solar-map-ph | OSM cross-match | `detection/scan/match_against_osm.py`, `match_against_osm_regions.py` | Cross-references detections against OSM tags to compute %-new vs known | Reuse to cross-match inferred design-speed segments against OSM `maxspeed` / `highway` tags -> gives a free "posted vs OSM" baseline and a labeled-positive pool for training. |
| solar-map-ph | HF publish + verify-release scripts | `scripts/publish_to_huggingface.py`, `scripts/verify_v11_release.py`, `scripts/verify_clf.py` | Dataset/model-card push to HF; release-readiness gate; classifier smoke verify | Reuse `verify_clf.py` as the model smoke test in CI; `publish_to_huggingface.py` if the C4 model/dataset is published. |
| solar-map-ph | CI deterministic-hash job pattern | `CLAUDE.md` (CI section) + `.github/workflows/ci.yml` touch-list note | CI `touch`es committed artifacts in dependency order so `make hash-verify` doesn't rebuild the model (no torch/network in CI) | Copy this CI trick — it's the non-obvious part of making a CNN-head repo pass CI without GPU. Update the touch-list when the classifier version bumps. |

## Top 3 highest-leverage transplants

1. **`solar-map-ph/detection/scan/ncr_scan.py` + `train_classifier.py`** — the entire grid->fetch->CLIP-embed->LR-score->resumable-JSONL->tiered-GeoJSON loop plus the LOSO-trained head. This is the C4 model's skeleton; swap the Esri-tile fetch for a Mapillary-frame fetch and the "tile" unit for a "road segment", keep everything else. Highest leverage by far.

2. **`pipeline/_gee_init.py` + the personal `.ee-key.json` (project `poised-honor-217909`)** — zero-rewrite SA-key-first GEE init for the land-cover/built-up prefilter layer, with the `region_scan.py` ESA-WorldCover prefilter showing exactly how to wire it. Personal key confirmed clean.

3. **MODEL_CARD.md + the `Makefile` hash-verify chain + `verify_release.py` gate** — the reproducibility + civic-honesty discipline ADB will expect: byte-pinned deterministic build, pinned deps, intended-use/out-of-scope ("advisory, not enforcement") section, and the em-dash/AI-jargon pre-ship sweep.
